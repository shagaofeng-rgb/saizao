import Link from "next/link"; import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { publishedProducts } from "@/lib/published-content"; export const revalidate=60; export default async function Products(){const items=await publishedProducts();return <><SiteHeader/><main id="main-content" className="catalog-page"><header className="catalog-hero"><p className="eyebrow">PRODUCT LIBRARY</p><h1>Fragrance solutions for your next product.</h1><p>Explore published product information, application routes and technical files from Sai Zhao Fragrance.</p></header><section className="catalog-grid">{items.length?items.map(item=><article className="catalog-card" key={item.id}>{item.cover_url&&<img src={item.cover_url} alt={item.title}/>}<div><p className="eyebrow">{item.content_categories?.name??item.application??"PRODUCT"}</p><h2>{item.title}</h2><p>{item.summary}</p><Link className="text-link" href={`/products/${item.slug}`}>View product</Link></div></article>):<p className="catalog-empty">Product information will be published here shortly.</p>}</section></main><SiteFooter/></>}