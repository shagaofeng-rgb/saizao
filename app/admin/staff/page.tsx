import { redirect } from "next/navigation"; import { getAdminSession, canManageStaff } from "@/lib/admin-session"; import { StaffManager } from "@/components/admin/StaffManager"; export const dynamic="force-dynamic"; export default async function AdminStaff(){const session=await getAdminSession();if(!session)redirect("/admin/login");if(!canManageStaff(session.role))redirect("/admin");return <StaffManager/>;}